/* Edit before publishing — see README.md */
window.REPAIRFRONT = {
  /** Deployed RepairFront app URL (no trailing slash) */
  apiBase: "https://app.repairfront.com",
  /** Staff sign-in page */
  appLoginUrl: "https://app.repairfront.com/login",
  /**
   * Where these HTML files are hosted (no trailing slash).
   * Leave empty for relative links (GitHub Pages subfolder, local preview).
   * Set to your Google Sites published URL if embedding pages in iframes.
   */
  siteBase: "",
};
